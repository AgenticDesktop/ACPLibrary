- 所有异步操作统一接受 `CancellationToken ct = default` 参数以支持可取消性。
- 对外暴露的 API 均通过接口定义（`IJsonRpcDispatcher`、`IRequestTracker`），实现类默认构造函数接受可选依赖并提供默认实现。
- 消息路由通过 `ConcurrentDictionary<string, Func<...>>` 按 method 名称注册处理器，区分 Request 与 Notification 两类处理器集合。
- JSON 序列化统一使用 `JsonOptions.Default` 作为共享配置，避免重复配置。