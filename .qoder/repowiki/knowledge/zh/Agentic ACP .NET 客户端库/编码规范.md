- 所有公共 API 通过接口（如 IAcpClient、IAgentTransport）暴露，便于测试与替换实现
- JSON-RPC 消息统一使用 JsonRpcRequest/JsonRpcResponse/JsonRpcNotification 模型，错误通过 JsonRpcError 封装
- 扩展点采用 Handler 接口模式（Permission/FileSystem/Terminal），在 InitializeAsync 之前赋值
- 异步操作统一使用 async/await 与 Task 返回值，避免同步阻塞