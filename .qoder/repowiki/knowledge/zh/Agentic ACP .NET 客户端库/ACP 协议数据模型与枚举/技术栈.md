C# 10+ record/class + System.Text.Json 序列化；使用 `[JsonPropertyName]` 精确映射 ACP JSON 字段名，`[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` 省略空值，`[JsonStringEnumConverter]` + `[JsonStringEnumMemberName]` 将枚举序列化为小写字符串。