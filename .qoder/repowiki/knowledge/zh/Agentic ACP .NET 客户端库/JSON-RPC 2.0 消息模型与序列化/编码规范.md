- 所有 JSON-RPC 数据模型均使用 C# record 定义，属性采用 init-only 自动属性保证不可变序列化结果。
- JSON 字段名通过 `[JsonPropertyName]` 显式声明，避免依赖默认命名约定。
- 可选字段统一使用 `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` 在序列化时省略 null 值。
- 参数与可选数据统一使用 `JsonElement?` 类型承载原始 JSON 片段，延迟具体反序列化。